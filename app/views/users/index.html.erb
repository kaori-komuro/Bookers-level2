<%= render 'books/header', books: @books %>

<%= render 'books/profile', books: @books %>

<div class="col-lg-9">
<h2>Users</h2>
<table class='table table-hover table-inverse'>
  <thead>
    <tr>
      <th>image</th>
      <th>name</th>
      <th colspan="3"></th>
    </tr>
  </thead>
<tbody>
<% @user.each do |user| %>
<tr>
  <td>
    <%= attachment_image_tag user, :profile_image, size: "40x40", format: 'jpeg', fallback: "sample-author1.jpg" %>
  </td>
  <td>
<%= user.username %>
  </td>
  <td>
<%= link_to "Show", user_path(user.id), class: "user_#{user.id}" %>
  </td>
<% end %>
</tbody>
</table>

<br>
</div>
</div>
</div>

    <footer>
  <div class='container'>
    <div class='row'>
      <div class='col-xs-3 col-xs-offset-9'>
        <p>CopyRight Infratop.inc</p>
      </div>
    </div>
  </div>